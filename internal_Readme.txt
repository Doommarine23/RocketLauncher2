"If the engine is a modern ZDoom based engine, use the maplump name, e.g. 'MAP01', otherwise if it's a more oldschool engine, use the map number, e.g. '01'

  Modern ZDoom Engines: use maplump name, e.g. 'MAP01'.
  Oldschool Engines: use map number, e.g. '01'.
  
  Turok 1: use map name, e.g. 'level06'. 
  NOTE: If a map is embedded deeper, e.g. 'levels/myturok/', you MUST add it, e.g. 'myturok/level06'
"